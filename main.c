#include "libft/libft.h"

struct map
{
	/*
	 * struct room->next;
	 *
	 */
};

struct room
{
	/*
	 * bool наличия муравьев
	 * char* номер комнаты
	 * волна
	 */
};

1. пройтись по комнатам и напистаь их волну


int main()
{
	/* Инициализация:
	 * - структура для карты:
	 * 		int ants;
	 * - обнуление структуры
	 * Чтение карты:
	 * -
	 * Валидация
	 * Алгоритм
	 * Вывод:
	 * Error management:
	 * - чистка карты;
	 */

	return 0;
}